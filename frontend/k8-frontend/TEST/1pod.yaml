apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod
  labels:
    app: myapp
    # type: front-end
spec:
  containers:
    - name: nginx-pod-container
      image: nginx
# apiVersion: v1
# kind: Pod
# metadata:
#   name: myapp-pod
#   labels:
#     app: myapp
# spec:
#   containers:
#     - name: nginx-container
#       image: nginx